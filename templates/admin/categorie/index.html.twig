{% extends 'base-admin.html.twig' %}

{% block title %}Liste des catégories{% endblock %}

{% block body %}
    <h1>Liste des catégories</h1>

    <div class="categorie-index">
     {% for categorie in categories %}
        <div class="categorie">
            <div class="infos-categorie">
                <div class="id-slug">
                    <div class="id">{{ categorie.id }} </div>
                    <div class="slug">{{ categorie.slug }}</div>
                </div>

                <div class="nom">
                    <h2>{{ categorie.nom }}</h2>
                </div>

                <div class="projets-par-categorie">
                    {% for projet in categorie.projets %}
                    {{ projet.nom }}<br>
                    {% endfor %}
                </div>
            </div>

            <div class="actions">
                <a class="btn btn-blanc" href="{{ path('app_admin_categorie_show', {'id': categorie.id}) }}">show</a>
                <a class="btn btn-jaune" href="{{ path('app_admin_categorie_edit', {'id': categorie.id}) }}">edit</a>
            </div>
        
        
        
        </div>
    {% endfor %}
    </div>

    

    <a href="{{ path('app_admin_categorie_new') }}">Create new</a>
{% endblock %}
