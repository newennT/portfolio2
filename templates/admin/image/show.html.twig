{% extends 'base-admin.html.twig' %}

{% block title %}Image{% endblock %}

{% block body %}
    <h1>Image</h1>
    <h2>{{ image.projet.nom }}</h2>
    <div class="image-show-admin">
        <div class="champ">
            <div class="intitule">Texte alt</div>
            <p>{{ image.texteAlt }}</p>
        </div>

        <div class="champ">
            <div class="intitule">Ordre</div>
            <p>{{ image.ordre }}</p>
        </div>

        <div class="champ">
            <div class="intitule">Path</div>
            <p>{{ image.path }}</p>
            <img src="{{ asset('uploads/' ~ image.path) }}" alt="Image">

        </div>
        

    
    </div>
    


    <a class="btn btn-blanc" href="{{ path('app_admin_image_index') }}"><< Retourner à la liste des images</a>

    <a class="btn btn-jaune" href="{{ path('app_admin_image_edit', {'id': image.id}) }}">Modifier</a>

    {{ include('admin/image/_delete_form.html.twig') }}
{% endblock %}
